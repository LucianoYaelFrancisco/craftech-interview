apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: interview-app
  labels:
    app: frontend
spec:
  type: ClusterIP
  
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  
  selector:
    app: frontend
  
  sessionAffinity: None
